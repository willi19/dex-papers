# Rendered paper teaser figures — kept LOCAL only (copyright; do not upload to GitHub).
# overview.html / overview/*.html reference these but they are not tracked.
overview_assets/

# Paper PDFs — kept LOCAL only (copyright; do not upload to GitHub).
# The browser's PDF buttons work locally; on a fresh public clone they are disabled.
*.pdf
